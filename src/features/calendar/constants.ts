/** Höhe einer Stunde im Zeitraster (px). */
export const HOUR_HEIGHT = 48

/** Breite der Zeitleisten-Spalte (px). */
export const GUTTER_WIDTH = 56

/** Gesamthöhe des Zeitrasters für 24 Stunden (px). */
export const TOTAL_HEIGHT = HOUR_HEIGHT * 24

/** Raster-Schritt beim Erstellen neuer Termine per Klick (Minuten). */
export const SLOT_MINUTES = 30
